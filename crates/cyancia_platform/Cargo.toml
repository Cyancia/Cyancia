[package]
name = "cyancia_platform"
version = "0.1.0"
edition = "2024"

[dependencies]
log.workspace = true
windows = { workspace = true, features = ["Win32_Foundation", "Win32_UI_WindowsAndMessaging"] }
